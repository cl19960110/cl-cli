// 普通加载路由
// import <%= data.name %> from './<%= data.name %>.vue'
// 懒加载路由
const <%= data.name %> = () => import('./<%= data.name %>.vue')
export dafault {
  path: '<%= data.lowerName %>',
  name: '<%= data.neme %>',
  component: '<%= data.neme %>',
  children: []
}